# Format: <namespace> <label> <value>
# For example, in order to sample a pod in the namespace testns, with a label named app which has the value worker, use the following:
# testns app worker
default app generator
default app receiver
